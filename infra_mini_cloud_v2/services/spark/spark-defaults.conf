# ── Iceberg Extensions ─────────────────────────────────────────────────────────
spark.sql.extensions                                    org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions

# ── Polaris REST Catalog ───────────────────────────────────────────────────────
spark.sql.catalog.polaris                               org.apache.iceberg.spark.SparkCatalog
spark.sql.catalog.polaris.type                          rest
spark.sql.catalog.polaris.uri                           http://polaris:8181/api/catalog
spark.sql.catalog.polaris.credential                    root:s3cr3t
# spark.sql.catalog.polaris.warehouse                     rustfs_catalog
spark.sql.catalog.polaris.scope                         PRINCIPAL_ROLE:ALL
spark.sql.catalog.polaris.header.Polaris-Realm          POLARIS

# ── S3FileIO (SDK v2) — acesso ao RustFS via Polaris ──────────────────────────
spark.sql.catalog.polaris.io-impl                       org.apache.iceberg.aws.s3.S3FileIO
spark.sql.catalog.polaris.s3.endpoint                   http://rustfs:9000
spark.sql.catalog.polaris.s3.access-key-id              rustfs
spark.sql.catalog.polaris.s3.secret-access-key          rustfs123
spark.sql.catalog.polaris.s3.path-style-access          true

# ── AWS credentials (SDK v2) ───────────────────────────────────────────────────
# Todas as flags -D em uma unica linha — nao duplicar a chave
spark.driver.extraJavaOptions                           -Daws.accessKeyId=rustfs -Daws.secretAccessKey=rustfs123 -Daws.region=us-east-1 -Dlog4j.logger.org.apache.iceberg.rest.RESTMetricsReporter=OFF
spark.executor.extraJavaOptions                         -Daws.accessKeyId=rustfs -Daws.secretAccessKey=rustfs123 -Daws.region=us-east-1

# ── S3A / RustFS (SDK v1) — apenas com hadoop-aws no classpath ────────────────
# Remover comentario somente se usar Hive Catalog em vez do Polaris
# Requer: hadoop-aws-3.3.4.jar + aws-java-sdk-bundle-1.12.262.jar
# ATENCAO: incompativel com iceberg-aws-bundle (conflito SDK v1 vs v2)
#spark.hadoop.fs.s3a.endpoint                            http://rustfs:9000
#spark.hadoop.fs.s3a.access.key                          rustfs
#spark.hadoop.fs.s3a.secret.key                          rustfs123
#spark.hadoop.fs.s3a.path.style.access                   true
#spark.hadoop.fs.s3a.connection.ssl.enabled              false
#spark.hadoop.fs.s3a.impl                                org.apache.hadoop.fs.s3a.S3AFileSystem
#spark.hadoop.fs.s3a.aws.credentials.provider            org.apache.hadoop.fs.s3a.SimpleAWSCredentialsProvider
#spark.hadoop.fs.s3.impl                                 org.apache.hadoop.fs.s3a.S3AFileSystem
#spark.hadoop.fs.s3.endpoint                             http://rustfs:9000
#spark.hadoop.fs.s3.access.key                           rustfs
#spark.hadoop.fs.s3.secret.key                           rustfs123
#spark.hadoop.fs.s3.path.style.access                    true